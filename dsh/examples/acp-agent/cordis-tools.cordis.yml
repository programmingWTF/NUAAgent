# Add the self-referential Cordis tools without changing the base ACP tool
# presentation mode.
- id: base
  name: '@nuaagent/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: cordis-host-runner
            name: '@nuaagent/cordis-host-runner'
          - id: tool-cordis
            name: '@nuaagent/tool-cordis'
