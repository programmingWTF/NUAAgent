# Test-only composition of Codex, the Bundle-supplied default Claude provider,
# and two named Claude instances. It never invokes a model or product process.
- id: fixture
  name: './fixture.ts'

- id: subagent
  name: '@nuaagent/subagent'

- id: subprocess
  name: '@nuaagent/subprocess-local'

- id: subagent-codex
  name: '@nuaagent/subagent-codex'

- id: subagent-claude-primary
  name: '@nuaagent/subagent-claude-code'
  config:
    providerName: claude-primary

- id: subagent-claude-secondary
  name: '@nuaagent/subagent-claude-code'
  config:
    providerName: claude-secondary

- id: tool-subagent-codex
  name: '@nuaagent/tool-subagent'
  config:
    provider: codex
    toolName: subagent_codex
    backgroundMode: one-shot
    maxDepth: 'provider-managed'

- id: tool-subagent-claude-code
  name: '@nuaagent/tool-subagent'
  config:
    provider: claude-code
    toolName: subagent_claude_code
    backgroundMode: one-shot
    maxDepth: 'provider-managed'

- id: tool-subagent-claude-primary
  name: '@nuaagent/tool-subagent'
  config:
    provider: claude-primary
    toolName: subagent_claude_primary
    backgroundMode: one-shot
    maxDepth: 'provider-managed'

- id: tool-subagent-claude-secondary
  name: '@nuaagent/tool-subagent'
  config:
    provider: claude-secondary
    toolName: subagent_claude_secondary
    backgroundMode: one-shot
    maxDepth: 'provider-managed'

- id: agent-spine
  name: '@nuaagent/agent-spine-demo'
  config:
    agents:
      - id: main
        provider: mock
        model: mock-delegate
        cwd: !!js process.cwd()
    persona: 'This composition test must not start a model turn.'
    workspaceContext: false

- id: persistence
  name: '@nuaagent/session-persistence-jsonl'
  config:
    root: './.sessions'

- id: checkpoint-policy
  name: '@nuaagent/session-checkpoint-policy'
