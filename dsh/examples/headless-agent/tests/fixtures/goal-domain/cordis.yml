# Test-only composition: create one goal through a Loader-mounted step consumer.
- id: cli-mock-llm
  name: '../cli-mock-llm.ts'

# Managed child-process groups for the bash executor (spawn/kill/output plumbing).
- id: subprocess
  name: '@nuaagent/subprocess-local'

- id: bash
  name: '@nuaagent/bash-local'

- id: goal
  name: '@nuaagent/goal'
  config:
    defaultMaxGoalRounds: 11

- id: seed-goal
  name: './seed-goal.ts'

- id: agent-spine
  name: '@nuaagent/agent-spine-demo'
  config:
    agents:
      - id: main
        provider: cli-mock
        model: cli-mock
        cwd: !!js process.cwd()
    persona: 'Test the persisted goal domain.'
    workspaceContext: false

- id: persistence
  name: '@nuaagent/session-persistence-jsonl'
  config:
    root: './.sessions'
    compression: none

- id: checkpoint-policy
  name: '@nuaagent/session-checkpoint-policy'
