# Keyless Agent Teams composition over the real headless app and deterministic fixture adapter.
- id: base
  name: '@nuaagent/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@nuaagent/llm-deepseek'
        disabled: true
      - id: tool-subagent-control
        name: '@nuaagent/tool-subagent-control'
        disabled: true
      - id: tool-subagent-report
        name: '@nuaagent/tool-subagent-report'
        disabled: true
      - id: tool-subagent
        name: '@nuaagent/tool-subagent'
        config:
          provider: spawn
          toolName: subagent
          backgroundMode: one-shot
          maxDepth: 1
      - id: tool-subagent-fork
        name: '@nuaagent/tool-subagent'
        config:
          provider: fork
          toolName: subagent_fork
          backgroundMode: one-shot
          maxDepth: 1
      - insert:
          - id: agent-team
            name: '@nuaagent/experimental-agent-team'
          - id: tool-agent-team
            name: '@nuaagent/experimental-tool-agent-team'
          - id: team-fixture-llm
            name: './tests/fixtures/team-llm.mjs'
