# NUAAgent 愿景

NUAAgent 是面向南京航空航天大学（NUAA）校内师生的 AI 编程助手，让校内免费大模型 API 在 Web GUI 与命令行（headless）中开箱即用。

它起源于把 DeepSeek Harness 品牌化移植，并针对南航网关的内容规范要求、单并发限制等现实约束专门优化。

## 指导原则

- **开箱即用**：默认体验就是最好的体验——一键接入南航 API，无需手动改配置。
- **不污染上游**：南航定制全部收敛在 `dsh-adapter/`，便于将来同步上游 DeepSeek Harness。
- **一切皆插件**：行为改动优先走 Cordis 插件与配置，不改 `agent-loop` 主循环。

## 当前状态

- 已支持 Web GUI（默认 `http://127.0.0.1:3080`）与 headless 一次性任务。
- 已内置网关内容适配、代理回退、单并发约束声明、配置合并启动器。

## 方向

优先：

- 稳定适配南航 API（并发 = 1、网关过滤）这一核心路径。

下一步：

- 完善文档与社区模板。
- 按需补充更多代理预设。

贡献规则：

- 一个 PR = 一个议题/主题。
- 提交风格：`类型(作用域): 描述`（见 CONTRIBUTING.md）。
